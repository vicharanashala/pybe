# Week 2026-W27 — Pilot kickoff week

> **Status:** Synthetic data — real pilot launches later in Q3 2026.

---

## Headline metrics

- Reach: 0 students (pilot scheduled to begin after cohort recruitment)
- Active days: 0 / 5
- Median session length: 0 min
- Level 2 reached: 0 %
- Mastery reached: 0 %
- Pyodide runs: 0 (event tracker fires on first run)
- Feedback collected: 0 ratings
- INV-PB-3 violations (capped scores): **0** ✓
- INV-PB-5 hits (shallow PRs accepted): **0** ✓

### What's working

- The build is green. `npm run build` produces a 297 KB JS bundle (90 KB gzip), 33 KB CSS. Lighthouse-clean.
- All 29 invariants have at least one runnable test. The Phase-8 anti-removal guardrail fires correctly.
- 32 case studies on disk; the LLM generator runs end-to-end with the Mock fallback.
- The 8-event analytics taxonomy is in place. `LocalStorageTracker` persists under `pybe:analytics:v1`.

### What's blocking

- **Cohort not yet recruited.** Phase 9's pilot requires a confirmed set of 20+ students.
- **Server endpoint not configured.** `PYBE_LLM_ENDPOINT` is unset; `npm run generate-cases` runs on the Mock generator only. This is by design for v1 but is the first item on the v1.1 backlog.
- **Analytics dashboard UI is read-only** — the `/weekly-review` page lists events but does not aggregate them yet. Phase 11 follow-up.

### Decisions made this week

1. **Tag the build as v1.0.0.** All Phase 0–10 acceptance criteria are green; 163/163 tests pass. The release is open-source with `docs/CHANGELOG.md` documenting v0.0.1 → v1.0.0.
2. **Defer all stretch features.** Per Sir's mandate in the visioning session, Horcruxes / Time Stone / Sorting Hat are gated on "4 weeks of pilot data" — we currently have 0.
3. **Pin the LLM generator to Mock for the pilot.** Until the institution's data-protection review signs off on outbound LLM calls, the Mock generator is the default. Switching to a real LLM requires only setting the three `PYBE_LLM_*` env vars.

### Next week's 1–3 ships (v1.0.1 patch release)

- [ ] **Open-source the v1.0.0 release notes** — copy from `docs/CHANGELOG.md` into a GitHub release.
- [ ] **Add a small "report a bug" link** to the footer so pilot participants can flag issues with one click.
- [ ] **Bundle an SVG logo** — currently the landing page has text only; a clean mark would help with the launch.

### Stretch (Phase 11+) — _only after 4 weeks of pilot data_

- [ ] No stretch features started. INV-PB-3 (no cap) and the Metaphor Engine (INV-PB-4) remain locked at their v1 contracts.

### Action items / owners

| Item | Owner | Due |
|---|---|---|
| Recruit pilot cohort | Sir | next week |
| Add bug-report footer link | Engineering | next week |
| Design SVG logo | Designer (TBD) | next week |

---

_Next review: 2026-W28. Same template, same file location._